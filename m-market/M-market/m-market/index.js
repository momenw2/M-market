    // const profileLink = document.createElement('a');
    // profileLink.setAttribute('href', 'html/profile.html');
    // profileLink.innerHTML = 'Profile';

    // const loginLink = document.querySelector('.nav-right a[href="html/login.html"]');
    // const signUpLink = document.querySelector('.nav-right a[href="html/signup.html"]');

    // function replaceLinks() {
    // // Replace the login link with the profile link
    // if (loginLink) {
    //     loginLink.parentNode.replaceChild(profileLink.cloneNode(true), loginLink);
    // }

    // // Replace the signup link with the profile link
    // if (signUpLink) {
    //     signUpLink.parentNode.replaceChild(profileLink.cloneNode(true), signUpLink);
    // }
    // }

    // document.addEventListener('DOMContentLoaded', function() {
    // // Listen for clicks on the login and signup links
    // if (loginLink) {
    //     loginLink.addEventListener('click', replaceLinks);
    // }
    // if (signUpLink) {
    //     signUpLink.addEventListener('click', replaceLinks);
    // }
    // });
